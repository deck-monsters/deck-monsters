[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/server/Dockerfile"
watchPatterns = ["packages/server/**", "packages/engine/**"]

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
