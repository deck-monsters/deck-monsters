/** Milliseconds per HP point healed while resting out of the ring. */
export const TIME_TO_HEAL_MS = 120000; // 2 minutes per hp

/** Milliseconds per level a dead monster must wait before respawning. */
export const TIME_TO_RESURRECT_MS = 600000; // 10 minutes per level
