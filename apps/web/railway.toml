[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @deck-monsters/engine build && pnpm --filter @deck-monsters/web build"

[deploy]
startCommand = "pnpm --filter @deck-monsters/web start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
